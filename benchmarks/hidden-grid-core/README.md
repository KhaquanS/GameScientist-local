# Hidden Grid core pack

This pack contains the first GameScientist task, `pacman-unknown`. The public
side defines only the observation and action interface. The private side holds
the fixed board configuration and a known-good solver used to validate that
the task is solvable.
