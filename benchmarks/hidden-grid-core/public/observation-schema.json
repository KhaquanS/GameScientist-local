{
  "type": "object",
  "required": ["grid", "stepCount", "remainingSteps", "terminal"],
  "properties": {
    "grid": {"type": "array", "items": {"type": "string"}},
    "stepCount": {"type": "integer", "minimum": 0},
    "remainingSteps": {"type": "integer", "minimum": 0},
    "terminal": {"type": "boolean"}
  }
}
