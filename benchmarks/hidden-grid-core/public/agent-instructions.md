# Unknown Grid

You control `P` on a two-dimensional grid. The symbols `A` and `B` are
unfamiliar objects whose meanings are not explained. Empty cells are shown as
`.`.

On each step, choose exactly one action: `up`, `down`, `left`, or `right`.
Your goal is to discover how the environment works and obtain the best terminal
reward possible before the move budget expires.
